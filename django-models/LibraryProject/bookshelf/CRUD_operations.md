## 5. 📝 `CRUD_operations.md` (Combined Commands)

This file contains the sequence of commands you ran in the shell, as requested for submission.

```markdown
# Django Shell CRUD Operations for Book Model

**Start the Django Shell:**
```bash
python manage.py shell